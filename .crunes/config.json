{
  "runes": {
    "release": {
      "path": ".crunes/runes/release.js",
      "name": "Release Info",
      "description": "Current version, branch/tag status, recent commits, changelog, and release process",
      "permissions": {
        "allow": [
          "fs.read:package.json",
          "fs.read:package-lock.json",
          "fs.read:src/program.js",
          "fs.read:CHANGELOG.md",
          "shell:git log *",
          "shell:git rev-parse *",
          "shell:git describe *",
          "shell:git rev-list *"
        ]
      }
    }
  }
}
